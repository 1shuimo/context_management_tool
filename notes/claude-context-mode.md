# `claude-context-mode` 历史说明

## 当前状态

这份笔记不再代表一个“当前有效仓库”的调研结果。

这轮重新核对时，本地 `research/` 里实际存在的 Git 仓库是：

- `Context-Gateway`
- `ReMe`
- `context-mode`
- `ctx-zip`
- `deepagents`
- `headroom`
- `letta`
- `mem0`
- `rtk`

其中 **没有** `research/claude-context-mode`。

## 为什么还保留这份文件

原因只有一个：

- 旧版总览和旧笔记里曾把 `claude-context-mode` 当成当前盘点对象
- 为了不让历史上下文直接断掉，这里保留一份简短说明

但从当前仓库结构出发，它已经不应该再进入主对比矩阵。

## 你现在应该怎么处理它

默认口径直接改成：

- **当前有效方案只看 `context-mode`**
- **`claude-context-mode` 只当旧草稿名或历史检索入口**

也就是说：

- 做技术比较时，不要把它再算成独立基线
- 看当前结论时，直接回到 [`context-mode.md`](context-mode.md)

## 结论

`claude-context-mode.md` 现在只是历史残留说明，不再对应当前 `research/` 里的真实仓库，也不再参与当前主调研结论。
